# array::push_if_cap_available
- **From:** 📦 Core / Array

## Description
Push if capacity available
## Format:
```
array::push_if_cap_available ->&array ->$val
```

⚠️ It may reject silent if size isn't within capacity, not recommended